"""
Você pode usar def sem return quando a função só executa uma ação
em vez de devolver um valor.
Retorno de valores das funções;
None, não valor;


def soma(a, b):
    return a + b # Retorna o valor;
    # acaba a execução da função;
    print(1+1)

variavel = soma(1, 2)
variavel = int("1")
Executa uma função;
soma1 = soma(2, 2)
soma2 = soma(3, 3)
print(soma1 + soma2)

def soma(x, y):
    if x > 10:
        return [10, 20] # Um por função, apenas um valor;
"""
